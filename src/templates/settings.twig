{% extends '_layouts/cp' %}

{% block content %}

<div>
    <h2>Language Redirect for CraftCMS</h2>
    <p>This plugin allows you redirect your users to the desired url based on user browser preferences.</p>
</div>
<hr>
<div>
    <h2>How to set up</h2>
    <p>You can configure the plugin by creating your own config file with desired settings in <code>/config/language-redirect.php</code>
    <pre>
    <code>
    return [
        'defaultLanguage' => 'en-GB',
        'urls' =>  [
            'ca'    => '/ca/',
            'ca-ES' => '/ca/',
            'es'    => '/es/',
            'es-ES' => '/es/',
            'en-GB' => '/en/',
            'en-US' => '/en/',
            'en'    =>  '/en/'
        ]
    ];
    </code>
    </pre>
</div>

{% endblock %}